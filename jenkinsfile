pipeline {
  agent any

  stages {
    stage('Checkout') {
      steps {
        checkout scm
      }
    }

    stage('Hello from Git') {
      steps {
        echo 'Hello Jenkins from Git'
      }
    }
  }
}